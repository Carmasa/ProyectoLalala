<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.shape.Rectangle?>

<StackPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
           prefHeight="1080.0" prefWidth="1920.0" alignment="CENTER">
   
   <!-- Fondo semi-transparente -->
   <Rectangle width="1920.0" height="1080.0" fill="#000000" opacity="0.7" />
   
   <!-- Contenido del diálogo -->
   <VBox alignment="CENTER" spacing="30.0" maxWidth="600" maxHeight="500"
         style="-fx-background-color: linear-gradient(to bottom right, #667EEA, #764BA2, #F093FB); 
                -fx-background-radius: 20; -fx-padding: 50;
                -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 25, 0, 0, 5);">
      
      <!-- Icono -->
      <Label text="🎲" style="-fx-font-size: 60px;" />
      
      <!-- Pregunta -->
      <Label text="¿Quieres jugar de nuevo?" 
             style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: white;
                    -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.6), 3, 0, 1, 1);" 
             textAlignment="CENTER" />
      
      <!-- Subtexto -->
      <Label text="¡Vamos a mezclar las cartas otra vez!" 
             style="-fx-font-size: 18px; -fx-text-fill: #FFE5B4;" 
             textAlignment="CENTER" wrapText="true" />
      
      <!-- Botones -->
      <HBox alignment="CENTER" spacing="30">
         <Button fx:id="btnSi" text="✨ ¡SÍ, JUGAR! ✨" 
                 style="-fx-background-color: linear-gradient(to bottom, #00C851, #007E33);
                        -fx-text-fill: white; -fx-font-size: 18px; -fx-font-weight: bold;
                        -fx-padding: 15 35; -fx-background-radius: 30; -fx-cursor: hand;
                        -fx-effect: dropshadow(gaussian, rgba(0,200,81,0.6), 10, 0, 0, 4);" />
         
         <Button fx:id="btnNo" text="🏠 Volver al Menú" 
                 style="-fx-background-color: linear-gradient(to bottom, #FF4444, #CC0000);
                        -fx-text-fill: white; -fx-font-size: 18px; -fx-font-weight: bold;
                        -fx-padding: 15 35; -fx-background-radius: 30; -fx-cursor: hand;
                        -fx-effect: dropshadow(gaussian, rgba(255,68,68,0.6), 10, 0, 0, 4);" />
      </HBox>
   </VBox>
   
</StackPane>
